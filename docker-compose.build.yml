services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    image: ${REGISTRY:-dronhome13}/web:${TAG:-latest}

  team19-web:
    build:
      context: ./team-19-web
      dockerfile: Dockerfile
    image: ${REGISTRY:-dronhome13}/team19-web:${TAG:-latest}

  nao-robot-api:
    build:
      context: ./team-19-social-robot/naoRobotAPI
      dockerfile: Dockerfile
    image: ${REGISTRY:-dronhome13}/nao-robot-api:${TAG:-latest}

  skeleton-finder-api:
    build:
      context: ./team-19-social-robot/skeletonFinderAPI
      dockerfile: Dockerfile
    image: ${REGISTRY:-dronhome13}/skeleton-finder-api:${TAG:-latest}

  translator:
    build:
      context: ./team-19-social-robot/translator
      dockerfile: Dockerfile
    image: ${REGISTRY:-dronhome13}/translator:${TAG:-latest}

  exercise-config-service:
    build:
      context: ./team-19-social-robot/ExerciseConfigService
      dockerfile: Dockerfile
    image: ${REGISTRY:-dronhome13}/exercise-config-service:${TAG:-latest}